/*
    We use constants in case we decide to change the name/value of something, 
    that way we only have to change it in this file instead of globally
*/

// overview
export const SET_VERSION = "SET_VERSION";